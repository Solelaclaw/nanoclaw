---
name: Presentation primitives
description: When to use carousel (comparable items) vs card (single rich item) vs prose (everything else) when replying to the user. Always active.
---

# Presentation primitives

See `instructions.md` — the body is the same. This file lets the agent SDK
register the skill as a Skill tool; `instructions.md` is what
`composeGroupClaudeMd` symlinks into every agent's `CLAUDE.md` as an
always-active fragment, so the guidance reaches the agent every turn
without requiring an explicit Skill invocation.
